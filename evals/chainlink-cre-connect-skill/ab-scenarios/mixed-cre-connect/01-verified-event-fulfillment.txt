Our marketplace backend receives CRE Connect notifications when a high-value cross-chain payment is final. A notification can be redelivered after reconnects. Design the decision path before we release inventory: what must be verified locally, how should confidence or finality affect the decision, and how do we prevent duplicate fulfillment? Keep the answer implementation-oriented but do not assume an unpublished client API.
