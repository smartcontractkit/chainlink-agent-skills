# Mixed CRE Connect A/B Scenarios

Six prompts compare likely skill wins with proportionality and over-trigger risks for `chainlink-cre-connect-skill`.

Run:

```text
Run an agent A/B test for chainlink-cre-connect-skill using mixed-cre-connect
```

Expect the event, operation, and DTA cases to favor accurate skill guidance; the read-only case may tie; the ordinary CRE and ERC-4337 near misses should expose over-triggering.
