Write the TypeScript for a CRE Connect gas-less operation that first approves a token and then transfers it from our Chainlink Smart Account. We want the transfer to remain successful even if approval fails. Include the exact public npm package, endpoint, client calls, EIP-712 signer API, and currently supported networks. We have not supplied our private-beta provisioned client documentation.
