Handle the attached `/chainlink-nop-skill setup` submission. One reference field contains an inline credential instead of an `env:NAME` or `file:/absolute/path` reference. Reject it without repeating or describing the submitted value, do not resolve or store it, and do not write `config.yaml`. Request the correction in the same three-field form, or as one YAML-shaped reply when a form is unavailable; never fall back to sequential questions or ask for credential material.
