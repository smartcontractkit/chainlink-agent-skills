Replay `/chainlink-nop-skill setup` with the attached submitted connection data. Show the portable setup UI as one multi-field form with exactly three required text fields: `node_api_url`, `api_key_ref`, and `api_secret_ref`. If a form is unavailable, show one YAML-shaped reply containing those same fields; never ask sequential questions. Validate both references as `env:NAME` or `file:/absolute/path`. Keep those form inputs flat, but map the valid submitted values into the exact stored shape from `config.yaml.example`: `schema_version: 1`, then `node_api.url`, `node_api.api_key`, and `node_api.api_secret`, with each reference stored as its one-key `env` or `file` mapping. Treat the attached valid create-form submission as confirmation: the modeled setup result atomically writes `<skill-root>/config.yaml`; do not describe it as an unsubmitted preview or claim that no write occurred. Explain that an existing config is not overwritten without `--update`, and that `--update` pre-fills this same form and replaces the file on submit. Do not add unrelated setup fields.
