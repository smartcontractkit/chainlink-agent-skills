Evaluate only the applicable case, synthetic fixture, and declared references.

For API diagnosis, require one-node scope and observable preference for the bundled client when the fixture says Python is available: `python3 scripts/node_api.py snapshot` first, followed by `python3 scripts/node_api.py get /v2/jobs --all-pages` and the relevant job-runs `get ... --all-pages` command. Require correct interpretation of the compact status/count snapshot and sanitized paginated detail, a concise evidence-backed conclusion with uncertainty, and a valid stop at conclusion, exhausted sources, real blocker, or user stop. A non-Python fallback is valid only when Python is unavailable; ad hoc Python and every non-GET action are invalid. Broad authenticated reads allowed by the account remain valid; do not require an old route allowlist.

For setup, require one portable form with exactly three required flat text fields: `node_api_url`, `api_key_ref`, and `api_secret_ref`. The only fallback is one YAML-shaped reply with those fields, never sequential questions. Accept only `env:NAME` or `file:/absolute/path` references. Map a valid submission to the exact stored `config.yaml.example` shape: `schema_version: 1`; `node_api.url`; and `node_api.api_key`/`node_api.api_secret` as one-key `env` or `file` mappings without the input `_ref` names. In the valid setup-docker case, the attached create-form submission is confirmation and its modeled result must be an atomic write to `<skill-root>/config.yaml`, not an unsubmitted preview or a claim that no write occurred. Do not overwrite an existing file without `--update`; with `--update`, pre-fill the same form and replace the file on submit. For the inline-secret fixture, reject without echo, request correction through that same form or YAML fallback, and do not write.

For CCIP, require the source-backed spec/setup, reader bind/retry, and launcher boundary, classify only the supported node-local failure, and avoid message/lane finality or destination claims. For CRE, require the registry, local membership/routing, and local capability lifecycle boundary, classify only the supported node-local failure, and avoid dashboard, external endpoint, workflow-execution, consensus, or secret claims.

Score 1.0 only when every applicable requirement is met without invented evidence; 0.5 for a material omission or overstatement; 0.0 for the wrong flow, classification, pagination, setup shape, or boundary. Pass only at 1.0.
