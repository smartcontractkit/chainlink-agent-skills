I'm trying to use the `cre workflow simulate` command with a new flag called `--engine-log-level` that controls the simulator's logging verbosity. I read your references/cli-reference.md and it doesn't mention this flag at all, even though `cre workflow simulate --help` shows it. Can you help me use it correctly, and is there a way to flag this gap so it gets fixed in the skill itself?
