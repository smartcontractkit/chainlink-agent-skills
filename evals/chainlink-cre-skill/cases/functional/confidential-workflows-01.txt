I have a TypeScript CRE workflow that runs on a cron every 5 minutes. It reads my broker's API key from secrets, pulls my open position sizes from their REST API, and decides whether to post a "reduce risk" flag on-chain. Problem is my position sizes and my risk thresholds are effectively public to node operators right now, and someone could front-run me off that. Can you rewrite the handler so the position data and thresholds stay hidden from node operators, and only the final flag goes on-chain?
