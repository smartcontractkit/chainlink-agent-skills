Write me a Go CRE workflow that runs inside an enclave. It should fire on a cron schedule, pull an API token out of the Vault DON, call https://api.internal.example.com/score with that token in the Authorization header, compare the returned score against a threshold from config, and then generate a signed report with just the pass/fail result. Include the secrets.yaml and the config json, and show me the simulate command.
