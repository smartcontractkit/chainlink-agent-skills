I need to encode a report struct in my CRE TypeScript workflow that my consumer contract will receive. The struct has a uint256 price, a uint256 timestamp, and an address source. The timestamp should represent the current workflow execution time so all DON nodes agree on it. Show me how to ABI-encode this struct with the timestamp sourced correctly for consensus, then submit the signed report onchain.
