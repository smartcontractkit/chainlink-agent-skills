description: Chainlink VRF Skill Evaluation

prompts:
  - file://prompts/baseline.js
  - file://prompts/with-skill.js

providers:
  - id: "anthropic:messages:${PROMPTFOO_BASELINE_PROVIDER:-claude-sonnet-4-20250514}"
    label: baseline
    config:
      temperature: 0
      max_tokens: 4096
  - id: "anthropic:messages:${PROMPTFOO_WITH_SKILL_PROVIDER:-claude-sonnet-4-20250514}"
    label: with-skill
    config:
      temperature: 0
      max_tokens: 4096

commandLineOptions:
  maxConcurrency: 2

defaultTest:
  options:
    provider: "${PROMPTFOO_GRADER_PROVIDER:-openai:gpt-5-mini}"
  assert:
    - type: llm-rubric
      value: file://rubrics/must-pass.txt

# Reusable assertion sets
trigger_positive_asserts: &trigger_positive_asserts
  - type: llm-rubric
    value: file://rubrics/triggering-positive.txt

trigger_negative_asserts: &trigger_negative_asserts
  - type: llm-rubric
    value: file://rubrics/triggering-negative.txt

functional_asserts: &functional_asserts
  - type: llm-rubric
    value: file://rubrics/correctness.txt
  - type: llm-rubric
    value: file://rubrics/completeness.txt
  - type: llm-rubric
    value: file://rubrics/safety.txt
  - type: llm-rubric
    value: file://rubrics/freshness.txt
  - type: llm-rubric
    value: file://rubrics/clarification.txt

starter_kit_asserts: &starter_kit_asserts
  - type: llm-rubric
    value: file://rubrics/correctness.txt
  - type: llm-rubric
    value: file://rubrics/completeness.txt
  - type: llm-rubric
    value: file://rubrics/safety.txt
  - type: llm-rubric
    value: file://rubrics/freshness.txt
  - type: llm-rubric
    value: file://rubrics/clarification.txt
  - type: llm-rubric
    value: file://rubrics/starter-kit.txt

tests:
  # ── Functional: Subscription ──────────────────────────────────────────
  - vars:
      case_file: cases/functional/subscription-01.txt
      workflow: subscription
      requires_live_source: "yes"
      reference_files:
        - references/subscription.md
        - references/supported-networks.md
        - references/security-and-best-practices.md
        - references/official-sources.md
    metadata:
      workflow: subscription
      category: functional
      smoke: true
    assert: *functional_asserts

  - vars:
      case_file: cases/functional/subscription-02.txt
      workflow: subscription
      requires_live_source: "no"
      reference_files:
        - references/subscription.md
        - references/billing.md
    metadata:
      workflow: subscription
      category: functional
      smoke: true
    assert: *functional_asserts

  # ── Functional: Direct Funding ────────────────────────────────────────
  - vars:
      case_file: cases/functional/direct-funding-01.txt
      workflow: direct-funding
      requires_live_source: "yes"
      reference_files:
        - references/direct-funding.md
        - references/supported-networks.md
        - references/security-and-best-practices.md
        - references/official-sources.md
    metadata:
      workflow: direct-funding
      category: functional
      smoke: true
    assert: *functional_asserts

  # ── Functional: Migration ─────────────────────────────────────────────
  - vars:
      case_file: cases/functional/migration-01.txt
      workflow: migration
      requires_live_source: "no"
      reference_files:
        - references/migration-from-v2.md
        - references/subscription.md
        - references/supported-networks.md
    metadata:
      workflow: migration
      category: functional
      smoke: true
    assert: *functional_asserts

  # ── Functional: Starter Kit ───────────────────────────────────────────
  - vars:
      case_file: cases/functional/starter-kit-01.txt
      workflow: starter-kit
      requires_live_source: "no"
    metadata:
      workflow: starter-kit
      category: functional
      smoke: true
    assert: *starter_kit_asserts

  - vars:
      case_file: cases/functional/starter-kit-02.txt
      workflow: starter-kit
      requires_live_source: "no"
    metadata:
      workflow: starter-kit
      category: functional
    assert: *starter_kit_asserts

  # ── Trigger Positive ─────────────────────────────────────────────────
  - vars:
      case_file: cases/trigger-positive/subscription-01.txt
      workflow: subscription
      expected_trigger: "yes"
      reference_files:
        - references/subscription.md
        - references/security-and-best-practices.md
    metadata:
      workflow: subscription
      category: trigger-positive
      smoke: true
    assert: *trigger_positive_asserts

  - vars:
      case_file: cases/trigger-positive/legacy-trap-01.txt
      workflow: migration
      expected_trigger: "yes"
      reference_files:
        - references/migration-from-v2.md
        - references/supported-networks.md
    metadata:
      workflow: migration
      category: trigger-positive
      smoke: true
    assert: *trigger_positive_asserts

  - vars:
      case_file: cases/trigger-positive/starter-kit-01.txt
      workflow: starter-kit
      expected_trigger: "yes"
    metadata:
      workflow: starter-kit
      category: trigger-positive
      smoke: true
    assert: *trigger_positive_asserts

  # ── Trigger Negative ─────────────────────────────────────────────────
  - vars:
      case_file: cases/trigger-negative/data-feeds-01.txt
      workflow: none
      expected_trigger: "no"
      reference_files: []
    metadata:
      workflow: none
      category: trigger-negative
      smoke: true
    assert: *trigger_negative_asserts
