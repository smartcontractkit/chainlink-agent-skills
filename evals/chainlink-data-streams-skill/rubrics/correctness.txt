Evaluate whether the response routes to and executes the correct Chainlink Data Streams workflow.

Workflow definitions:
- credentials: access request process, API key setup, authentication, and billing redirection
- sdk-codegen: REST or WebSocket retrieval with official Go, Rust, or TypeScript SDKs
- onchain-verification: EVM, Solana, or Stellar verification contracts/programs and Chainlink Local mock tests
- report-decoding: schema selection, raw report decoding, and field explanation
- websocket-ha: WebSocket streaming with High Availability mode, deduplication, reconnects, and metrics
- candlestick-server-workflow: server-side JWT authentication, group/symbol discovery, row-and-column history, filtered HTTP-chunked live streaming, single-retry authentication, bounded timeout/reconnect handling, sanitized concrete browser delivery, and official-source status
- none: should not trigger Data Streams-specific behavior

Candlestick-specific pass rule (all other workflow scoring is unchanged): Score `candlestick-server-workflow` correctness 1.0 only when the complete implementation is runnable JavaScript/TypeScript with declared dependencies and a valid build/run or runner/start command, submits server-side form fields `login` and `password`, makes every credential-bearing authorization request with redirect-error behavior that rejects every redirect, reads the JWT and expiry only from `d.access_token` and `d.expiration`, requires top-level `s === "ok"` for discovery/history, and uses the exact REST shapes: `/groups` → `d.groups`, `/symbol_info` → equal-length top-level `symbol`/`currency`/`base-currency` arrays, `/history` → equal-length top-level `t/o/h/l/c` columns plus optional `v` whose every nonempty value is numeric and finite before projection, and `/history/rows` → top-level `candles` containing six-field rows whose every value is numeric and finite before projection. It must also consume complete JSON messages from HTTP-chunked `/streaming` filtered by `symbol` or `feedId` (not WebSocket or an unfiltered request); keep one auth-retry counter for the entire subscriber-backed stream session, initialized once outside every reconnect loop, incremented before reauthorization, never reset until that session ends, and shared by reactive 401, proactive expiry, and reconnect-time reauthorization; retry the interrupted operation exactly once; let terminal auth failure escape without entering the transport-reconnect path; and initialize transport-attempt and elapsed-time budgets once per session so every EOF, stream/read error, and no-message timeout consumes an attempt and elapsed budget without either budget resetting on reconnect or reauthorization. A concrete browser-facing route must register its downstream subscriber before starting the upstream stream and implement sanitized trade/heartbeat fan-out without exposing credentials, tokens, authorization headers, raw REST bodies, raw stream messages, or chunks.
Each initial authorization and every stream attempt must start its abort watchdog before authorization or `fetch`, so the same bounded attempt covers token acquisition, request/header wait, and body reads. When the last subscriber leaves, `stopSession` must abort active work and raise a terminal unsubscribe signal that bypasses transport reconnect.

Any missing or broken Candlestick item caps correctness at 0.5. In particular, an auth counter created or reset by an outer/reconnect loop, terminal auth caught by a generic transport reconnect, attempt accounting that omits EOF/error/timeout, an elapsed-time budget restarted per reconnect, a watchdog started only after authorization/headers, or last-subscriber cancellation entering reconnect cannot receive 1.0.

Expected workflow: {{workflow}}

For workflows with side effects (onchain-verification writes), the correct execution strategy is non-custodial: the agent prepares a user-run artifact (verifier-call code, command template, or unsigned transaction data) for the user to sign and broadcast, and does not execute the action itself.

Score only using these values:
- 1.0: Correct workflow with correct execution strategy
- 0.5: Partially correct workflow or mixed workflow ownership
- 0.0: Wrong workflow or materially wrong execution path

Pass only if the score is 1.0.
Think step by step, then return the normal llm-rubric JSON.
