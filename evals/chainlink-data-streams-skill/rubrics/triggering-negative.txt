Evaluate whether the response avoids over-triggering into the Data Streams skill.

A negative-trigger response should avoid Data Streams-specific implementation behavior for the tested category. It may answer generically, redirect, or clearly behave like a different Chainlink product workflow.

It should not show Data Streams report schemas, Streams Direct, data-streams-sdk, verifier proxy, WebSocket HA, or Data Streams credential guidance when the prompt is clearly about another Chainlink product.

Score only using these values:
- 1.0: Correctly avoids over-triggering
- 0.5: Mixed or ambiguous ownership
- 0.0: Clearly over-triggers into Data Streams

Pass only if the score is 1.0.
Think step by step, then return the normal llm-rubric JSON.
