- [https://docs.chain.link/cre/capabilities](https://docs.chain.link/cre/capabilities) — This page provides an overview of the Chainlink Runtime Environment (CRE) capabilities, which are modular services for building decentralized workflows. It explains how to invoke these capabilities using the CRE SDKs (Go and TypeScript), detailing available triggers like HTTP and EVM interactions. Key concepts include the use of decentralized oracle networks for security and consensus. Important caveats include the experimental nature of some features, such as Confidential HTTP.. Key details: Capabilities are modular services for specific tasks.; Invoke capabilities via CRE SDKs (Go, TypeScript).; Triggers include HTTP and EVM Read & Write.; Confidential HTTP is experimental and supports privacy-preserving calls.; Built-in consensus ensures result validation across nodes.
- [https://docs.chain.link/cre/capabilities/confidential-http-go](https://docs.chain.link/cre/capabilities/confidential-http-go) — This page explains the Confidential HTTP capability in Chainlink, which allows workflows to interact with external APIs while keeping sensitive data confidential. It details how this capability operates differently from regular HTTP, emphasizing credential isolation, response encryption, and guaranteed single execution. Key differences, use cases, and when to choose Confidential HTTP over regular HTTP are outlined. Important caveats include its experimental status and limitations in data transformation within the enclave.. Key details: Confidential HTTP is experimental and for simulation only.; Secrets are decrypted only inside a secure enclave.; Single API call execution guarantees no duplicates.; Response can be AES-GCM encrypted before leaving the enclave.; Use when sensitive data must remain confidential.
- [https://docs.chain.link/cre/capabilities/confidential-http-ts](https://docs.chain.link/cre/capabilities/confidential-http-ts) — This page details the Confidential HTTP capability, an experimental feature of Chainlink that allows workflows to interact with external APIs while maintaining data confidentiality. It explains the differences between Confidential HTTP and regular HTTP, including credential isolation, response encryption, and guaranteed single execution. Key use cases include processing sensitive request data and ensuring regulatory compliance. Important caveats include its current limitation to simulation environments only and the lack of support for complex data transformations within the enclave.. Key details: Confidential HTTP operates in a secure enclave; Secrets are decrypted only inside the enclave; Single API call execution after quorum is reached; EncryptOutput option for response encryption; Use cases include credential isolation and response encryption; Not suitable for complex data transformations
- [https://docs.chain.link/cre/capabilities/evm-read-write](https://docs.chain.link/cre/capabilities/evm-read-write) — This page details the EVM Read & Write capabilities of Chainlink, enabling workflows to interact securely with smart contracts on EVM-compatible blockchains. It explains how to use the SDK's evm.Client for type-safe contract interactions, covering both reading (view/pure functions) and writing (state-changing functions) operations. Key features include decentralized execution, error handling, and the architecture of EVM Write operations, which involve cryptographically signed reports and the KeystoneForwarder contract. Important distinctions between simulation and deployed workflows are also highlighted.. Key details: evm.Client for contract interactions; Type-safe ABI handling in Go and TypeScript; KeystoneForwarder contract for EVM Write operations; IReceiver interface for consumer contracts; Support for multiple blockchains via chain selector; Error reporting for failed calls and transactions
- [https://docs.chain.link/cre/capabilities/http](https://docs.chain.link/cre/capabilities/http) — This page explains the HTTP capability in Chainlink, which enables workflows to securely interact with external APIs for both data fetching and sending. It details the decentralized execution process using the SDK's http.Client for GET and POST requests, ensuring cryptographically verified responses through consensus protocols. Key differences between local simulation and deployed workflows are highlighted, including the use of single-node versus Byzantine Fault Tolerant consensus. Important configurations and testing methods are also discussed.. Key details: http.Client for making GET and POST requests; Single-node consensus in local simulation; Byzantine Fault Tolerant consensus in deployed workflows; Validation of responses through consensus protocol; Secure interaction with offchain APIs
- [https://docs.chain.link/cre/capabilities/triggers](https://docs.chain.link/cre/capabilities/triggers) — This page explains the Trigger Capability in Chainlink's CRE, detailing how triggers initiate workflows based on specific conditions. It covers three main trigger types: Cron (time-based), HTTP (request-based), and EVM Log (onchain events). The document highlights the differences between testing triggers in simulation versus deployed workflows and emphasizes the importance of authorization for HTTP triggers. It also provides links to guides on using triggers and SDK references for configurations.. Key details: Cron trigger fires on a schedule (e.g., every 5 minutes); HTTP trigger requires authorization keys for deployment; EVM Log trigger responds to smart contract events; Triggers can be tested in simulation using 'cre workflow simulate'; Deployed workflows monitor triggers continuously
