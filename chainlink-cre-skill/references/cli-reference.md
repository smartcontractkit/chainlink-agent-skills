- [https://docs.chain.link/cre/reference/cli](https://docs.chain.link/cre/reference/cli) — This page provides a comprehensive reference for the Chainlink Command Line Interface (CLI), detailing commands for project setup, workflow management, authentication, and secrets management. Key patterns include initializing projects, generating Go contract bindings, and managing workflow lifecycles. Important caveats include the requirement to use CLI version v1.0.10 for compatibility. The page also outlines global flags applicable to all commands and typical development workflows.. Key details: Required CLI Version: v1.0.10; cre init - Initialize a new CRE project; cre generate-bindings - Generate Go bindings from contract ABI files; cre workflow simulate - Test workflows locally; cre secrets create - Create new secrets from a YAML file
- [https://docs.chain.link/cre/reference/cli/account](https://docs.chain.link/cre/reference/cli/account) — This page provides guidance on managing linked public key addresses for workflow operations using the Chainlink CRE CLI. It covers commands such as 'cre account link-key', 'cre account list-key', and 'cre account unlink-key', detailing their usage, prerequisites, and interactive flows. Important caveats include the necessity of being logged in, having a funded wallet for gas fees, and the destructive nature of unlinking keys, which deletes associated workflows.. Key details: Commands: cre account link-key, list-key, unlink-key; Requires authentication via cre login; Must run from a directory with project.yaml; .env must contain CRE_ETH_PRIVATE_KEY; Wallet must be funded with ETH for gas fees; Unlinking is destructive and cannot be undone; Global flags: --env, --target, --project-root, --verbose
- [https://docs.chain.link/cre/reference/cli/authentication](https://docs.chain.link/cre/reference/cli/authentication) — This page details the authentication methods for the Chainlink CRE CLI, including browser-based login and API key authentication. It provides commands like 'cre login', 'cre logout', and 'cre whoami', along with instructions for creating and using an API key. Important caveats include the requirement for Early Access approval for API key usage and the need to secure API keys properly. The page also summarizes the best use cases for each authentication method.. Key details: cre login: opens browser for interactive login; cre logout: revokes tokens and deletes credentials; cre whoami: displays current account details; API key requires Early Access approval; Set CRE_API_KEY environment variable for non-interactive authentication
- [https://docs.chain.link/cre/reference/cli/project-setup-go](https://docs.chain.link/cre/reference/cli/project-setup-go) — This page provides instructions for setting up Chainlink CRE projects using CLI commands. It covers the 'cre init' command for initializing projects and adding workflows, as well as 'cre generate-bindings' for generating Go bindings from contract ABI files. Key patterns include interactive and non-interactive modes for project setup and the structure of generated bindings. Important caveats include the requirement for authentication and the need to run specific commands after adding new ABI files.. Key details: cre init initializes a new project or adds a workflow; cre generate-bindings generates Go bindings from contract ABI files; Interactive mode recommended for project setup; Global flags include --env, --target, --project-root, --verbose; Generated bindings placed in contracts/{chain-family}/src/generated/
- [https://docs.chain.link/cre/reference/cli/project-setup-ts](https://docs.chain.link/cre/reference/cli/project-setup-ts) — This page provides instructions for initializing Chainlink CRE projects using the CLI command 'cre init'. It covers both interactive and non-interactive modes for project setup, including flags for project name, template ID, and workflow name. The page emphasizes the importance of being logged in to the CRE CLI and outlines the typical workflow for setting up TypeScript projects, including installing dependencies and writing workflow code. Key configuration files, such as project.yaml and workflow.yaml, are also mentioned.. Key details: Command: cre init initializes a new project or adds a workflow; Global flags: --env, --target, --project-root, --verbose; Interactive mode recommended for ease of use; Non-interactive mode allows for direct command execution; Dependencies installed using bun install; Project configuration files: project.yaml, workflow.yaml
- [https://docs.chain.link/cre/reference/cli/secrets](https://docs.chain.link/cre/reference/cli/secrets) — This page provides detailed instructions on managing secrets in the Chainlink CRE CLI, including commands to create, update, delete, and list secrets stored in the Vault DON. It emphasizes the use of YAML file formats for input and outlines the necessary flags and arguments for each command. Important caveats include the requirement to be logged in to the CLI and the permanent nature of deletions. The page also discusses the organization of secrets into namespaces and the use of multi-sig wallets.. Key details: Commands: cre secrets create, update, delete, list; Input format: YAML files for secrets management; Default namespace: 'main', with potential for custom namespaces; Permanent deletion of secrets; Global flags: --env, --target, --project-root, --verbose; Multi-sig support with --unsigned flag
- [https://docs.chain.link/cre/reference/cli/utilities](https://docs.chain.link/cre/reference/cli/utilities) — This page provides an overview of utility commands for the Chainlink CRE CLI, including how to update the CLI and check its version. Key commands include 'cre update' for updating to the latest version and 'cre version' for displaying the current version. Important caveats include ensuring version compatibility with the documentation and the need to update SDK dependencies for existing projects. The page also mentions global flags that can be used with commands.. Key details: cre update: updates the CLI to the latest version; cre version: prints the current version of the CLI; Global flags: --env, --target, --project-root, --verbose; Automatic version checks when running certain commands; Recommended CLI version: v1.0.11
- [https://docs.chain.link/cre/reference/cli/workflow](https://docs.chain.link/cre/reference/cli/workflow) — This page provides detailed instructions on managing workflows in Chainlink's CRE CLI, covering commands for simulating, deploying, activating, pausing, and deleting workflows. Key patterns include the usage of flags for each command, such as --broadcast for simulation and --unsigned for deployment. Important caveats include the need for authentication and the potential destructive nature of the delete command. The typical workflow lifecycle is outlined, emphasizing local development, simulation, deployment, and management.. Key details: cre workflow simulate <workflow-name-or-path> [flags]; cre workflow deploy <workflow-name-or-path> [flags]; cre workflow activate <workflow-name-or-path> [flags]; cre workflow pause <workflow-name-or-path> [flags]; cre workflow delete <workflow-name-or-path> [flags]; --broadcast flag for onchain writes; --unsigned flag to return raw transactions; Dry run by default for simulation; Destructive delete operation requires caution
