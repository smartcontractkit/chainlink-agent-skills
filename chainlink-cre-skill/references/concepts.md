- [https://docs.chain.link/cre/concepts/consensus-computing](https://docs.chain.link/cre/concepts/consensus-computing) — This page explains consensus computing within Chainlink's CRE, detailing how it ensures secure and reliable workflows by verifying operations through multiple independent nodes. It covers the importance of tamper-resistance, high availability, and trust minimization, as well as the automatic inclusion of decentralized consensus in CRE capabilities. Key patterns include the use of http.Client and evm.Client for API and blockchain interactions, respectively, and the distinction between consensus in simulation versus deployed workflows.. Key details: Consensus ensures tamper-resistance and high availability; Independent execution by multiple nodes for each operation; Byzantine Fault Tolerant (BFT) consensus protocol; Validation of API responses and blockchain interactions; Single-node model used in local simulation for testing; Multi-node consensus in deployed workflows
- [https://docs.chain.link/cre/concepts/finality-go](https://docs.chain.link/cre/concepts/finality-go) — This page explains Chainlink's finality model, detailing three confidence levels (LATEST, SAFE, FINALIZED) for blockchain data operations. It describes how these levels map to different chains and their implications for reading and writing data. Key patterns include using block depth for custom finality rules and understanding the response from write operations. Important caveats include the potential for transaction reorganization and the need for post-write verification for critical applications.. Key details: LATEST: no finality guarantees, use for non-critical operations; SAFE: unlikely to be reorganized, use for most operations; FINALIZED: irreversible, use for critical transactions; Custom block depths for regulatory compliance and historical verification; WriteReportReply indicates inclusion in a block, not finality; Transaction Manager automatically resubmits on reorg
- [https://docs.chain.link/cre/concepts/finality-ts](https://docs.chain.link/cre/concepts/finality-ts) — This page explains Chainlink's finality model, detailing three confidence levels (LATEST, SAFE, FINALIZED) for reading blockchain data and event monitoring. It describes how these levels map to different chains and outlines the implications for chain reads and writes, including custom block depths and reorganization handling. Important caveats include the need for post-write verification for critical transactions and the limitations of the SAFE level for chain reads.. Key details: LATEST: no finality guarantees, suitable for non-critical operations; SAFE: unlikely to be reorganized, good for most operations; FINALIZED: irreversible, used for critical transactions; Custom block depths allow tailored finality rules; WriteReportReply indicates inclusion in a block, not finality; Reorg handling with automatic resubmission and gas bumping
- [https://docs.chain.link/cre/concepts/non-determinism-go](https://docs.chain.link/cre/concepts/non-determinism-go) — This page explains the importance of determinism in Chainlink workflows, particularly in DON mode where multiple nodes must execute identical code paths to reach consensus. It outlines common pitfalls such as map iteration, JSON serialization, concurrency issues, and random number generation, providing best practices to avoid non-determinism. Key solutions include using sorted map keys, deterministic JSON serialization, and specific functions from the CRE SDK for time and randomness.. Key details: Sort map keys before iteration; Use encoding/json v1 for deterministic JSON serialization; Use proto.MarshalOptions{Deterministic: true} for Protocol Buffers; Process channels in a fixed, deterministic order; Use runtime.Now() for time operations; Use runtime.Rand() for random number generation; Request structured output from LLMs
- [https://docs.chain.link/cre/concepts/non-determinism-ts](https://docs.chain.link/cre/concepts/non-determinism-ts) — This page explains the importance of determinism in Chainlink workflows, particularly in DON mode where multiple nodes must execute identical code paths to reach consensus. It outlines common pitfalls such as unordered object iteration, non-deterministic Promise handling, and the use of JavaScript's time functions. Best practices include using Maps for ordered iteration, calling .result() in a deterministic order, and using runtime.now() for timestamps. The page also advises against relying on free-text responses from LLMs, recommending structured outputs instead.. Key details: Use Object.keys() for ordered iteration; Use Maps and Sets for guaranteed order; Call .result() in a fixed order; Use runtime.now() for timestamps; Avoid Promise.race() and Promise.any()
- [https://docs.chain.link/cre/concepts/typescript-wasm-runtime](https://docs.chain.link/cre/concepts/typescript-wasm-runtime) — This page explains how TypeScript workflows are compiled to WebAssembly (WASM) for execution in the Chainlink CRE environment. It details the compilation process using Javy, the differences between QuickJS and Node.js, and the limitations of using third-party libraries. Key patterns include the custom .result() method for SDK capabilities and the importance of testing library compatibility in simulation. It emphasizes the benefits of WASM, such as sandboxed and deterministic execution.. Key details: TypeScript → JavaScript → WASM compilation process; QuickJS provides core JavaScript functionality but not full Node.js API; Use of Noble for cryptographic functions; Custom .result() pattern for SDK capabilities; Verify third-party libraries for Node.js compatibility; Simulation with 'cre workflow simulate' for testing
